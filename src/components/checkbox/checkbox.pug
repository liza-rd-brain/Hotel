

mixin checkbox({text, key})
    -
        const CB_ITEMS ={ text:["Можно курить", "Можно с питомцами","Можно пригласить гостей (до 10 человек)"],key:"checkbox"}
        text = text|| CB_ITEMS.text
        key = key|| CB_ITEMS.key
    .checkbox-wrap
        - for (let i=0;i<text.length;i++)
            //будем принимать массив строк для чекбоксов и перебирать в цикле фор
            label(for=`${key}-${i}`) 
                p(class="checkbox-text") #{text[i]}
            input(type="checkbox" class="checkbox" name=`checkbox-${i}` id=`${key}-${i}`) 